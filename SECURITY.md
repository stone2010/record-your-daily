# 安全配置指南

## 🔐 密钥管理

### 密钥类型说明

| 密钥类型 | 变量名 | 用途 | 是否可暴露 |
|---------|--------|------|-----------|
| Publishable Key | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 客户端Auth认证、数据查询 | ✅ 可以暴露 |
| Secret Key | `SUPABASE_SERVICE_ROLE_KEY` | 服务端API、绕过RLS操作 | ❌ **绝对禁止暴露** |

### 安全规则

1. **Secret Key 绝对不能**：
   - 写入任何 `NEXT_PUBLIC_*` 前缀的变量
   - 提交到版本控制（已在 `.gitignore` 中排除）
   - 硬编码在前端代码中
   - 分享给任何人

2. **Publishable Key 可以**：
   - 用于客户端初始化 Supabase
   - 通过环境变量注入（`NEXT_PUBLIC_*`）
   - 暴露在浏览器中（受 RLS 保护）

### 配置验证

在应用启动时，会自动检查配置：

```typescript
// 客户端检查
if (!supabaseUrl || !supabaseAnonKey) {
  console.warn('[Supabase配置警告] 环境变量未配置');
}

// 服务端检查
if (!supabaseServiceKey) {
  console.error('[Supabase配置错误] 服务端密钥未配置');
}
```

## 🔒 数据库安全

### RLS策略

项目已配置完整的 Row Level Security 策略：

1. **profiles 表**：
   - 用户只能查看自己的档案
   - 用户可以更新自己的档案（除 is_vip 和 is_banned）

2. **diaries 表**：
   - 用户只能查看自己的日记（未被封禁）
   - 仅 VIP 用户可以创建/更新/删除日记
   - 被封禁用户无法进行任何操作

### 数据验证

| 验证项 | 规则 | 位置 |
|--------|------|------|
| 标题长度 | ≤ 200 字符 | 客户端 + 数据库约束 |
| 内容长度 | ≤ 20,000 字符 | 客户端 + 数据库约束 |
| 用户ID匹配 | 只能操作自己的数据 | RLS策略 |
| VIP状态检查 | 同步前验证 | 服务端API |

## 🛡️ 应用安全

### 前端安全

- ✅ 客户端数据验证（防止恶意输入）
- ✅ 操作限流（同步按钮10秒间隔）
- ✅ 用户输入清理（XSS防护）
- ✅ HTTPS传输（生产环境）

### 服务端安全

- ✅ JWT Token验证
- ✅ 用户封禁检查
- ✅ VIP状态验证
- ✅ 请求参数验证
- ✅ 错误日志记录

### 支付安全（待实现）

- ⚠️ 服务端签名验证
- ⚠️ 订单防重复处理
- ⚠️ 异步通知验证

## 🚀 部署安全

### Vercel部署

1. 在 Vercel 控制台设置环境变量：
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`

2. 确保：
   - Secret Key 使用**加密变量**存储
   - 不要在代码中打印密钥
   - 启用 HTTPS（Vercel 默认启用）

### 数据库部署

1. 在 Supabase 控制台：
   - 启用 RLS（已在迁移脚本中启用）
   - 配置适当的 Row Level Security 策略
   - 限制数据库访问权限

2. 推荐设置：
   - 启用数据库审计日志
   - 定期备份数据库
   - 启用网络访问控制

## 📋 安全检查清单

### 开发环境

- [ ] `.env.local` 文件不提交到版本控制
- [ ] Secret Key 不使用 `NEXT_PUBLIC_*` 前缀
- [ ] 代码中没有硬编码的密钥
- [ ] 开发服务器仅监听 localhost

### 生产环境

- [ ] 所有环境变量使用加密存储
- [ ] 启用 HTTPS
- [ ] 配置合适的 CORS 策略
- [ ] 启用数据库 RLS
- [ ] 定期更新依赖
- [ ] 启用错误监控和日志

### 运维

- [ ] 定期轮换密钥（至少每季度）
- [ ] 监控异常访问模式
- [ ] 定期审计数据库访问日志
- [ ] 制定数据备份和恢复策略

## ⚠️ 紧急情况处理

### 密钥泄露

如果 Secret Key 意外泄露：

1. **立即**在 Supabase 控制台生成新密钥
2. 更新所有环境变量
3. 检查是否有异常数据访问
4. 考虑轮换所有相关凭证

### 数据泄露

如果用户数据意外泄露：

1. **立即**通知受影响用户
2. 调查泄露原因
3. 修复安全漏洞
4. 加强安全措施

## 📞 安全联系

如有安全问题或漏洞报告，请联系开发团队。